#include<stdio.h>
#include<string.h>

int main(){
	char ch[100];
	printf("Enter the String:");
	gets(ch);
	int i=0;
	for(i=0;ch[i]!='\0';i++)
	{

	}
	printf("The length of the string is:%d",i);
	return 0;
}	